<div class="ui one column grid">
  <div class="row">
    <div class="column">
      <h1>New Message</h1>

      <%= form_with model: @message, local: true, class: 'ui form full-width' do |f| %>
        <%= render 'shared/error_messages', model: @message %>

        <div class="field">
          <%= f.label :recipient_username %>
          <div class="ui input">
            <%= f.text_field :recipient_username %>
          </div>
        </div>

        <div class="field">
          <%= f.label :subject %>
          <div class="ui input">
            <%= f.text_field :subject %>
          </div>
        </div>

        <div class="field">
          <%= f.label :body %>
          <div class="ui input">
            <%= f.text_area :body, rows: 10 %>
          </div>
        </div>

        <div class="field">
          <%= f.submit 'Send', class: 'ui button primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>